# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Search_Next_Train' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Search_Next_Train

    pod 'CalculateCalendarLogic'
    pod 'ISTimeline'
    #pod 'TimeLineControl'  
  
  target 'Search_Next_TrainTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Search_Next_TrainUITests' do
    # Pods for testing
  end

end
